<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_main"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetshopApp"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.PetsActivity" />
        <activity android:name=".activities.NewPetActivity" />
        <activity android:name=".activities.EditPetActivity" />
        <activity android:name=".activities.DeletePetActivity" />
        <activity android:name=".activities.SchedulingActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.EditProfileActivity" />
        <activity android:name=".activities.InactiveAccountActivity" />
        <activity android:name=".activities.NewSchedulingActivity" />
        <activity android:name=".activities.EditSchedulingActivity" />
        <activity android:name=".activities.DeleteSchedulingActivity" />
        <activity android:name=".activities.EndSchedulingActivity" />
        <activity android:name=".activities.ListAdminActivity" />
        <activity android:name=".activities.ClientsActivity" />

    </application>

</manifest>